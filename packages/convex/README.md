This package contains Convex schema and server functions.

Expected usage:
1. Copy/symlink `convex/` to repo root or configure Convex to use this directory.
2. Run `npx convex dev` for local generation of `_generated/*` files.
